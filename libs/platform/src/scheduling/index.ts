// ExclusiveJob is sourced from @quynhonsemiconductor/platform-runtime (single source of
// truth). Re-exported here so '@platform' consumers keep their import paths unchanged.
export { ExclusiveJob } from '@quynhonsemiconductor/platform-runtime';
